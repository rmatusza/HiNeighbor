import { createMuiTheme } from "@material-ui/core";

const Theme = createMuiTheme({
  palette: {
    primary: {
      main: "#ffffff",
    },
    secondary: {
      main: "#76323F",
    },
  },
  status: {
    danger: "orange",
  },
});
// "#565656"
export default Theme;
